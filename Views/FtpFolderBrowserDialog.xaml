<Window x:Class="StarRuptureSaveFixer.Views.FtpFolderBrowserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Browse Remote Folder" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Current Path -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Current Path:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
            <TextBox x:Name="CurrentPathTextBox" Text="/" IsReadOnly="True"
                     MinWidth="300" Padding="5" Background="#F0F0F0"/>
        </StackPanel>

        <!-- Navigation -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="UpButton" Content="Up" Click="UpButton_Click" Style="{StaticResource ActionButton}"/>
            <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" Style="{StaticResource ActionButton}"/>
            <Button x:Name="CancelOperationButton" Content="Cancel" Click="CancelOperationButton_Click"
                    Style="{StaticResource ActionButton}" Visibility="Collapsed"
                    Background="#F44336" Foreground="White"/>
        </StackPanel>

        <!-- Folder List -->
        <Grid Grid.Row="2">
            <ListBox x:Name="FolderListBox"
                     MouseDoubleClick="FolderListBox_MouseDoubleClick"
                     SelectionChanged="FolderListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <TextBlock Text="{Binding DisplayIcon}" FontFamily="Consolas"
                                       Foreground="{Binding IsDirectory, Converter={StaticResource DirectoryColorConverter}}"
                                       Width="50" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Name}" MinWidth="250" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding DisplaySize}" Foreground="Gray" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Loading overlay -->
            <Border x:Name="LoadingOverlay" Background="#80FFFFFF" Visibility="Collapsed">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                    <TextBlock x:Name="LoadingText" Text="Connecting..."
                               HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="3" x:Name="StatusTextBlock" Text="Ready"
                   Foreground="Gray" Margin="0,10,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SelectButton" Content="Select Folder" Click="SelectButton_Click"
                    Style="{StaticResource ActionButton}" IsEnabled="False"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            <Button Content="Cancel" Click="CancelButton_Click" Style="{StaticResource ActionButton}"/>
        </StackPanel>
    </Grid>
</Window>
