<UserControl x:Class="StarRuptureSaveFixer.Views.FtpUploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Button" x:Key="UploadButton">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBox" x:Key="FieldTextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="PasswordBox" x:Key="FieldPasswordBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- FTP Settings -->
        <GroupBox Grid.Row="0" Header="FTP Server Settings" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Host and Port -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Host:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FieldTextBox}" Margin="0,0,15,10"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Port:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding Port}"
                         Style="{StaticResource FieldTextBox}" Margin="0,0,0,10"/>

                <!-- Username -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Username:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FieldTextBox}" Margin="0,0,0,10"/>

                <!-- Password -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Password:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <PasswordBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                             x:Name="PasswordBox"
                             Style="{StaticResource FieldPasswordBox}" Margin="0,0,0,10"
                             PasswordChanged="PasswordBox_PasswordChanged"/>

                <!-- Remote Path with Browse Button -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Remote Path:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding RemotePath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldTextBox}" IsReadOnly="True"/>
                    <Button Grid.Column="1" Content="Select Session"
                            Command="{Binding BrowseRemoteCommand}"
                            Style="{StaticResource ActionButton}"
                            Margin="5,0,0,0"/>
                </Grid>

                <!-- Options and Buttons -->
                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,5,0,0">
                    <CheckBox Content="Use FTPS (TLS)" IsChecked="{Binding UseFtps}" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <CheckBox Content="Passive Mode" IsChecked="{Binding PassiveMode}" VerticalAlignment="Center" Margin="0,0,30,0"/>
                    <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Style="{StaticResource ActionButton}"/>
                    <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Style="{StaticResource ActionButton}"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource ActionButton}"
                            Background="#F44336" Foreground="White"
                            Visibility="{Binding IsUploading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- File Selection -->
        <GroupBox Grid.Row="1" Header="Select Save File to Upload" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Session Selector -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Session:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding Sessions}"
                              SelectedItem="{Binding SelectedSession}"
                              DisplayMemberPath="DisplayName"
                              MinWidth="200"/>
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource ActionButton}" Margin="15,0,0,0"/>
                </StackPanel>

                <!-- Files List (Single Selection) -->
                <ListBox Grid.Row="1" ItemsSource="{Binding AvailableFiles}"
                         SelectedItem="{Binding SelectedFile}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <TextBlock Text="{Binding FileName}" MinWidth="200" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="{Binding FileSizeDisplay}" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                <TextBlock Text="{Binding LastModifiedDisplay}" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Selected File Info -->
                <Border Grid.Row="2" Background="#FFF3E0" Padding="10" Margin="0,10,0,0" CornerRadius="3">
                    <TextBlock Text="{Binding SelectedFileInfo}" FontStyle="Italic"/>
                </Border>
            </Grid>
        </GroupBox>

        <!-- Upload Progress and Button -->
        <GroupBox Grid.Row="2" Header="Upload" Padding="10" Margin="0,15,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Progress Bar -->
                <ProgressBar Grid.Row="0" Value="{Binding UploadProgress}" Minimum="0" Maximum="100" Height="25"/>

                <!-- Status -->
                <TextBlock Grid.Row="1" Text="{Binding UploadStatus}" Margin="0,10,0,10"
                           HorizontalAlignment="Center"/>

                <!-- Upload Button -->
                <Button Grid.Row="2" Content="Upload to Server"
                        Command="{Binding UploadCommand}"
                        Style="{StaticResource UploadButton}"
                        HorizontalAlignment="Center"
                        IsEnabled="{Binding CanUpload}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
